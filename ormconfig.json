{
  "type": "databaseConfig",
  "host": "hostConfig",
  "port": portConfig,
  "username": "userConfig",
  "password": "passwordConfig",
  "database": "nameConfigDatabase",
  "entities": ["dist/**/*.entity.js"],
  "synchronize": false,
  "migrations": ["dist/migration/*.js"],
  "cli": {
    "migrationsDir": "migration"
  }
}
